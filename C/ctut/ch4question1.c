/* Write a function that takes two values a and b, then returns
   the value of a * b (that is, a times b.) */
int main ()
{
  int result = a_times_b (3, 6);
}

int a_times_b (int a, int b)
{
  int total;

  total = a * b;
  return total;
}

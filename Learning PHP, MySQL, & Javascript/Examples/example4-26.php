<?php // Using the ? operator
echo $fuel <= 1 ? "Fill da tank nao!" : "Yer fuel be fine";
	  // In this case, the program tests whether or not $fuel is <= 1.
	  // If that evaluates TRUE, perform the option to left of colon
	  // If FALSE, the option to right of colon
?>
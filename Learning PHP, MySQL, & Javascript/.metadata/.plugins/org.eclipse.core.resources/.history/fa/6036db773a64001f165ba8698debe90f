<?php //Including a PHP file only once - so ya don't get error messages due to accidentally
	  //including a library multiple times (ie: one library requires functions from
	  //another library, and thus includes the other library in its library, causing
	  //this second library to be included twice, causing an error)
?>